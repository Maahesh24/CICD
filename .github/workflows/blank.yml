# File: .github/workflows/workflow.yml

name: Deploy to Staging

on:
  workflow_dispatch:

jobs:
  my_job:
    name: deploy to staging
    runs-on: ubuntu-latest

    steps:
      # Azure Login using Service Principal
      - name: Azure Login
        uses: azure/login@v3
        with:
          creds: '{"clientId":"98df0580-39b3-4c8d-80ec-5143ae025113","clientSecret":"${{ secrets.AZURE_CLIENT_SECRET }}","subscriptionId":"669e5a53-2952-45fd-a198-1f4a
